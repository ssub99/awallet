/**
 * OTA (EAS Update) smoke test: change this string before each `eas update`,
 * then confirm the new value appears in Settings without reinstalling the app.
 */
export const OTA_TEST_MARKER = 'ota-1';
