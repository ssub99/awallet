# `npm run start:prod` 전용. 복사: `cp .env.production.local.example .env.production.local`
# 공통 변수는 루트 `.env`에 두고, prod 로컬에서만 덮어쓸 값만 여기에 적어도 됩니다.
EXPO_PUBLIC_AMPLITUDE_API_KEY_DEBUG=
EXPO_PUBLIC_AMPLITUDE_API_KEY_PROD=
